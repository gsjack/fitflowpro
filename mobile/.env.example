# FitFlow Pro - Mobile App Environment Configuration
# Copy this file to .env and configure for your environment

# ========================================
# API Configuration
# ========================================

# Backend API URL - IMPORTANT: Configure based on your environment
#
# For Web (browser):
#   FITFLOW_API_URL=http://localhost:3000
#
# For Physical Mobile Device (same WiFi network):
#   FITFLOW_API_URL=http://192.168.178.49:3000
#   (Replace 192.168.178.49 with your computer's local IP address)
#   Run 'ip addr show' or 'ifconfig' to find your IP
#
# For Android Emulator:
#   FITFLOW_API_URL=http://10.0.2.2:3000
#   (10.0.2.2 is a special alias for host machine in Android emulator)
#
# For iOS Simulator:
#   FITFLOW_API_URL=http://localhost:3000
#   (iOS simulator can access host's localhost)

FITFLOW_API_URL=http://localhost:3000

# ========================================
# How to find your local IP address:
# ========================================
#
# Linux/Mac:
#   ip addr show | grep "inet " | grep -v 127.0.0.1
#   OR
#   ifconfig | grep "inet " | grep -v 127.0.0.1
#
# Windows:
#   ipconfig | findstr IPv4
#
# The IP address will typically be:
#   - 192.168.x.x (home WiFi)
#   - 10.0.x.x (corporate network)
#   - 172.16-31.x.x (private network)
